# Minimal set for running spark.fun locally.
#
# backend/requirements.txt is the hosting image's kitchen-sink manifest (124
# lines: boto3, google-genai, torch tooling and so on) and installs slowly.
# These ten packages are what the code actually imports — verified by grepping
# every import in backend/. Use requirements.txt for the deployed image and
# this file on your machine.

fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2
pydantic==2.11.9
motor==3.3.1
pymongo==4.5.0
python-dotenv==1.1.1
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
httpx==0.28.1
eth-account==0.13.7

# tests
pytest==9.0.2
